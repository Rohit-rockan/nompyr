{
  "version": 2,
  "buildCommand": "cd frontend-isekai && npm install && npm run build",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://nompyr-1.onrender.com/api/:path*"
    }
  ]
}
